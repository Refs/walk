tree
- a node has one parent
- a node can have multiple children

directedGraph
- a node can have many parents
- a node can have many children

directedAcyclicGraph
- a node can have many parents
- a node can have many children
- no node can have an ancestor that is itself (cycle)

graph
- all node relationships are bidirectional

hybridGraph
- node relationships can bidirectional or directional